var adjustWidth;

adjustWidth = function() {
  return _($(".input-append")).each(function(el, idx) {
    var $el, $input, addonWidth, inputWidth;
    $el = $(el);
    $input = $el.find("input");
    inputWidth = parseInt($input.css('width')[0].offsetWidth);
    addonWidth = parseInt($el.find(".add-on")[0].offsetWidth);
    $input.css('width', inputWidth - addonWidth);
    return console.log(inputWidth, addonWidth);
  });
};

$(function() {
  return adjustWidth();
});
