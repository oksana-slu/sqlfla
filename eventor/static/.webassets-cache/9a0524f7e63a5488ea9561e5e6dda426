var adjustWidth;

adjustWidth = function() {
  var addonWidth, inputWidth;
  inputWidth = +$(".input-append input").css('width');
  addonWidth = +$(".input-append add-on").css('width');
  return console.log(inputWidth, addonWidth);
};

$(function() {
  return adjustWidth();
});
