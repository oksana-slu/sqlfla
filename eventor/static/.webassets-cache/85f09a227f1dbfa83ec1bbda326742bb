var adjustWidth;

adjustWidth = function() {
  var addonWidth, inputWidth;
  inputWidth = parseInt($(".input-append input").css('width'));
  addonWidth = parseInt($(".input-append .add-on").css('width'));
  return console.log(inputWidth, addonWidth);
};

$(function() {
  return adjustWidth();
});
